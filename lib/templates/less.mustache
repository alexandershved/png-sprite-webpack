{{#items}}
${{name}}: {{px.offset_x}} {{px.offset_y}} {{px.width}} {{px.height}} {{px.total_width}} {{px.total_height}};
{{/items}}

.sprite($sprite) {
  display: inline-block;
  width: extract($sprite, 3);
  height: extract($sprite, 4);
  background-position: extract($sprite, 1) extract($sprite, 2);
  background-repeat: no-repeat;
  background-size: extract($sprite, 5) extract($sprite, 6);
}

{{#items}}
{{class}} {
  .sprite(${{name}});
  background-image: url('~_images/{{{escaped_image}}}');
}

{{#escaped_image_2x}}
@media screen and (min-device-pixel-ratio: 2), screen and (min-resolution: 192dpi), screen and (min-resolution: 2dppx) {
  {{class}} {
    background-image: url('~_images/{{{escaped_image_2x}}}');
  }
}
{{/escaped_image_2x}}
  
{{/items}}